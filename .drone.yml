kind: pipeline
name: build monitoring

globals:
  - &tf_vars
    VERSION: 1.0.1
    
steps:
  - name: docker  
    image: plugins/docker
    settings:
      repo: sample/sw-hello-world
      tags: '1.0.${DRONE_BUILD_NUMBER}'
      dry_run: true
      
  - name: dockle
    image: goodwithtech/dockle
    commands:
      - dockle sample/sw-hello-world:'1.0.${DRONE_BUILD_NUMBER}'
       
  - name: trivy image
    image: aquasec/trivy
    environment:
      <<: *tf_vars
    commands:
     # - trivy fs .
      #- trivy image --exit-code 0 --severity LOW,MEDIUM,HIGH plugins/docker:19.03.8 
     # - trivy image --exit-code 1 --severity CRITICAL plugins/docker:19.03.8 
      #- trivy image --exit-code 0 --severity LOW,MEDIUM,HIGH -f json -o results01.json plugins/docker:19.03.8 
     # - trivy image --exit-code 1 --severity CRITICAL -f json -o results02.json plugins/docker:19.03.8
  
  - name: read file
    image: ubuntu:20.04
    environment:
      <<: *tf_vars
    commands:
     # - cat results01.json
     # - cat results02.json
      
      
      
   
   
      
